{{document-upload uploadDocument="uploadDocument"}}


{{file-upload uploadUrl=uploadUrl filename="files"
    buttonText="Upload files"
    hiddenName="fileId" hiddenValue=fileId
    uploaded="filesUploaded"}}
